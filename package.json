{
  "name": "allocco-landing",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name allocco-landing --branch main",
    "preview": "wrangler pages dev public"
  },
  "devDependencies": { "wrangler": "^4" }
}
