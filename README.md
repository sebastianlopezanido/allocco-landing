# allocco-landing

Landing provisoria de Allocco S.A., servida como sitio estático en Cloudflare Workers (static assets).

- `public/index.html`: la página. Nombre, email de contacto y animación "en construcción".
- Deploy: `npm install` una vez, `npx wrangler login` una vez, luego `npm run deploy`.
